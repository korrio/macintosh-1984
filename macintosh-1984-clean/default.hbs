<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{meta_title}}</title>
    
    {{!-- Theme assets --}}
    <link rel="stylesheet" href="https://unpkg.com/@sakun/system.css" />
    <link rel="stylesheet" type="text/css" href="{{asset "css/screen.css"}}" />
    
    {{!-- Ghost outputs important style and meta data --}}
    {{ghost_head}}
</head>
<body class="{{body_class}} {{#match @custom.title_font "Chicago"}}chicago-font{{/match}}{{#match @custom.title_font "Monaco"}}monaco-font{{/match}}">
    <div class="gh-viewport">
        <div class="blog-container">
            {{!-- Header --}}
            <header class="blog-header">
                <div class="window-controls">□ ▣</div>
                <h1 class="blog-title">
                    <span class="mac-icon"></span>
                    {{#if @site.logo}}
                        <a href="{{@site.url}}"><img class="site-logo" src="{{@site.logo}}" alt="{{@site.title}}" /></a>
                    {{else}}
                        <a href="{{@site.url}}">{{@site.title}}</a>
                    {{/if}}
                </h1>
                {{#if @site.description}}
                    <p class="blog-subtitle">{{@site.description}}</p>
                {{/if}}
            </header>

            {{!-- Main content area --}}
            <main id="site-main" class="site-main">
                {{{body}}}
            </main>

            {{!-- Footer --}}
            <footer class="footer">
                <p>© {{date format="YYYY"}} {{@site.title}} • Celebrating 40 Years of Innovation</p>
                <p>Built with System.css - Honoring the Classic Mac Experience</p>
                <p class="theme-version">Macintosh 1984 Theme v1.0.1</p>
                {{#if @site.secondary_navigation}}
                    <nav class="footer-nav">
                        {{navigation type="secondary"}}
                    </nav>
                {{/if}}
            </footer>
        </div>
    </div>

    {{!-- Ghost outputs important scripts and data --}}
    {{ghost_foot}}
    
    {{!-- Theme JavaScript --}}
    <script src="{{asset "js/app.js"}}"></script>
</body>
</html>